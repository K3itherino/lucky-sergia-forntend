import { useRef, useEffect, useState } from 'react';
import "./Dice.scss"


export default function Dice(props) {

    return (
        <div className={!props.sideBar ? 'dice':'dice blur'}>
            Cuming Soon...
        </div>
    );
}